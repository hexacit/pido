//||... File for enums ...||

// enum for user status >>>
//enum UserStatus { ONLINE, OFFLINE }

// enum for message type >>>
enum MessageType { MESSAGE_FAILED, MESSAGE_SUCCESS }

// enum for section type >>>
enum SectionType { SECTION_TYPE_BEAUTY, SECTION_TYPE_COFFEE }

// enum for static pages >>>
enum StaticPages {
  STATIC_PAGES_ABOUT_US,
  STATIC_PAGES_PRIVACY_POLICY,
  STATIC_PAGES_TERMS_OF_USE
}

// enum for info component type>>>
enum InfoComponentType {
  INFO_COMPONENT_TYPE_NO_NOTIFICATIONS,
  INFO_COMPONENT_TYPE_NO_INTERNET,
  INFO_COMPONENT_TYPE_NO_RESULTS,
}
enum ProductsMode {
 ALL,
  CATEGORY,
}
